package ngo.friendship.satellite.interfaces

import ngo.friendship.satellite.model.MedicineInfo

// TODO: Auto-generated Javadoc
/**
 * The listener interface for receiving onRetrieveMedicineData events.
 * The class that is interested in processing a onRetrieveMedicineData
 * event implements this interface, and the object created
 * with that class is registered with a component using the
 * component's `addOnRetrieveMedicineDataListener` method. When
 * the onRetrieveMedicineData event occurs, that object's appropriate
 * method is invoked.
 *
 * @see OnRetrieveMedicineDataEvent
`` */
interface OnRetrieveMedicineDataListener {
    /**
     * On data retrieve finished.
     *
     * @param medList the med list
     */
    fun onDataRetrieveFinished(medList: ArrayList<MedicineInfo?>?)
}