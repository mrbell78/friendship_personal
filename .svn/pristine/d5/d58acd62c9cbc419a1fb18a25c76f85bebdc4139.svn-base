package ngo.friendship.satellite.interfaces
// TODO: Auto-generated Javadoc
/**
 * The listener interface for receiving onInterviewUpload events.
 * The class that is interested in processing a onInterviewUpload
 * event implements this interface, and the object created
 * with that class is registered with a component using the
 * component's `addOnInterviewUploadListener` method. When
 * the onInterviewUpload event occurs, that object's appropriate
 * method is invoked.
 *
 * @see OnInterviewUploadEvent
`` */
interface OnInterviewUploadListener {
    /**
     * On interview upload finished.
     *
     * @param message the message
     */
    fun onInterviewUploadFinished(message: String?)
}