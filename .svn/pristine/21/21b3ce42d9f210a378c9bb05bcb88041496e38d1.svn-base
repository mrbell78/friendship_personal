package ngo.friendship.satellite.views;

import android.content.Context;
import android.graphics.Canvas;
import android.graphics.Typeface;
import android.util.AttributeSet;

import androidx.appcompat.widget.AppCompatButton;

/**
 * Created by SHAHADAT on 12/8/2016.
 */

public class MdiButton extends AppCompatButton {
    public MdiButton(Context context) {
        super(context);
        Typeface roboto = Typeface.createFromAsset(context.getAssets(), "fonts/materialdesignicons-webfont.ttf");
        this.setTypeface(roboto);
    }

    public MdiButton(Context context, AttributeSet attrs) {
        super(context, attrs);
        Typeface roboto = Typeface.createFromAsset(context.getAssets(), "fonts/materialdesignicons-webfont.ttf");
        this.setTypeface(roboto);
    }

    public MdiButton(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
        Typeface roboto = Typeface.createFromAsset(context.getAssets(), "fonts/materialdesignicons-webfont.ttf");
        this.setTypeface(roboto);
    }

    protected void onDraw(Canvas canvas) {
        super.onDraw(canvas);
    }
}
